export interface Commander {
  id: string;
  oracleId: string;
  name: string;
  colors: string[];
  colorIdentity: string[];
  imageUrl: string;
  backImageUrl?: string;
  edhrecUrl: string;
  scryfallUrl: string;
}
